


describe("User", function() {
    // var store, ctlr, Application = null;
    // var Application = new AxAgenda();

    // var usersStore = null, app = null;
    
    // beforeEach(function() {
    // 	if(!app) {
    // 	    app = AxAgenda.getApplication();
    // 	    expect(app !== null).toBeTruthy();
    // 	}
    // 	usersStore = Ext.StoreManager.lookup("userStore");
    // });
	       

    // it("should have a userstore", function() {
    // 	expect(usersStore !== null).toBeTruthy();
    // });
    // it("should have users",function(){
    // 	expect(store.getCount()).toBeGreaterThan(1);
    // });

    // it("should open the editor window", function(){
    // 	var grid = Ext.ComponentQuery.query('userlist')[0];

    // 	ctlr.editUser(grid,store.getAt(0));

    // 	var edit = Ext.ComponentQuery.query('useredit')[0];

    // 	expect(edit).toBeTruthy();
    // 	if(edit)edit.destroy();
    // });

});
   
